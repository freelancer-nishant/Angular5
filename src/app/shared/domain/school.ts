export interface School {
    id,
    client_id,
    code,
    name,
    abbrev,
    label,
    abbrev_ps,
    state_school_code
}

export interface SchoolAll {
    id,
    client_id,
    client_name
    code,
    name,
    abbrev,
    label,
    abbrev_ps,
    state_school_code
}
